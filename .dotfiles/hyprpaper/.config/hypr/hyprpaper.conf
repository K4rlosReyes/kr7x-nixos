# Preload wallpaper (loads it into memory)
preload = /home/carlos/Pictures/wp.jpg

# Or for all monitors:
wallpaper = ,/home/carlos/Pictures/wp.jpg

# Optional: Disable splash text
splash = false

# Optional: Enable IPC (allows runtime wallpaper changes)
ipc = on
